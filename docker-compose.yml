services:
  web:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    environment:
      - DATABASE_URL=file:/app/data/prod.db
    volumes:
      - db-data:/app/data
    restart: unless-stopped

volumes:
  db-data:
